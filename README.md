# dotfiles
My dotfiles.

Run `./link.sh` to deploy the configuration file.

If an `install.sh` script exists in a subdirectory, it can be used to install and configure the tool represented by that directory.